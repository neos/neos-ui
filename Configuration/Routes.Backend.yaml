-
  name: 'Hostframe'
  uriPattern: 'content'
  defaults:
    '@controller': 'Backend'
    '@action': 'index'
  appendExceedingArguments: true

-
  name: 'Deactivate / return to legacy backend'
  uriPattern: 'legacy'
  defaults:
    '@controller': 'Backend'
    '@action': 'deactivate'
