describe('Events middleware', () => {
    it('should expose the middleware factory by default');
    it('should expose a function to create event subscriber factories');
    it('should expose a function to create an initializer factory');
});
