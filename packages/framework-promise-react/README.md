# @neos-project/framework-promise-react

> React bindings for ECMAScript promises

This package provides a set of React [hooks](https://react.dev/reference/react/hooks) to let components interact with `Promise`s.
