{
  "name": "@neos-project/positional-array-sorter",
  "version": "2.0.0",
  "description": "Flexible array sorter that sorts an array according to a 'position' meta data.",
  "main": "./src/positionalArraySorter.ts",
  "license": "GNU GPLv3",
  "scripts": {
    "prebuild": "check-dependencies && yarn clean",
    "test": "yarn jest -w 2 --coverage",
    "test:watch": "yarn jest --watch",
    "build": "exit 0",
    "build:watch": "exit 0",
    "clean": "rimraf ./lib ./dist",
    "lint": "eslint src",
    "jest": "NODE_ENV=test jest"
  },
  "devDependencies": {
    "@neos-project/build-essentials": "2.0.0",
    "@neos-project/jest-preset-neos-ui": "2.0.0"
  },
  "jest": {
    "preset": "@neos-project/jest-preset-neos-ui"
  }
}
