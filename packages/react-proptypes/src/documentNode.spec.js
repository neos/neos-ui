import documentNode from './documentNode';

test(`should export a function.`, () => {
    expect(typeof (documentNode)).toBe('function');
});

test(`Figure out a way to test propTypes`, () => {
    expect(true).toBe(true);
});
