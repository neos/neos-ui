import editorOptions from './editorOptions';

test(`should export a function.`, () => {
    expect(typeof (editorOptions)).toBe('function');
});

test(`Figure out a way to test propTypes`, () => {
    expect(true).toBe(true);
});
