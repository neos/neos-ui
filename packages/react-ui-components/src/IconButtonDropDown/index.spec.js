import IconButtonDropDown from './index.js';

describe('<IconButtonDropDown/> (entry point)', () => {
    it('should export a Component.', () => {
        expect(typeof IconButtonDropDown).toBe('function');
    });
});
