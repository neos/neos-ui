import ListPreviewElement from '.';

describe('<ListPreviewElement/> (entry point)', () => {
    it('should export a Component.', () => {
        expect(typeof ListPreviewElement).toBe('function');
    });
});
