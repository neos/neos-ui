import MultiSelectBox from './index.js';

describe('<MultiSelectBox/> (entry point)', () => {
    it('should export a Component.', () => {
        expect(typeof MultiSelectBox).toBe('function');
    });
});
