{
  "include": [
    "src"
  ],
  "exclude": [
    "**/*.spec.*",
    "**/*.story.*"
  ],
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "lib-esm",
    "module": "esnext",
    "target": "es6",
    "moduleResolution": "node",
    "allowJs": true,
    // will fail on rebuild currently
    "incremental": false
  }
}
